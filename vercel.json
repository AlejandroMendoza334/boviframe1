{
  "version": 2,
  "builds": [
    {
      "src": "vercel-build.sh",
      "use": "@vercel/build-utils",
      "config": { "includeFiles": ["**/*"] }
    }
  ],
  "routes": [
    { "src": "/(.*)", "dest": "/build/web/$1" }
  ]
}
